# Required: Pulse API key for trace-service authentication
# Create one via:
#   ADMIN_KEY=test-admin-key PROJECT_NAME="SDK Dev" make sdk-create-api-key
# or:
#   curl -X POST http://localhost:3000/admin/projects \
#        -H "X-Admin-Key: test-admin-key" \
#        -H "Content-Type: application/json" \
#        -d '{"name":"SDK Dev"}'
PULSE_API_KEY=pulse_sk_...

# Optional: LLM provider API keys (tests skip providers without keys)
OPENAI_API_KEY=sk-...
ANTHROPIC_API_KEY=sk-ant-...
OPENROUTER_API_KEY=sk-or-...

# Local service URLs
# These point to the docker-compose stack or your own running services.
TRACE_SERVICE_URL=http://localhost:3000
TEST_SERVER_URL=http://localhost:3001
